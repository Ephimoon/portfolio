import React from 'react'

const Hero = () => {
    return (
        <section id="hero" className="min-h-screen snap-start flex items-center justify-center bg-blue-500">
            Hero Section
        </section>
    )
}

export default Hero